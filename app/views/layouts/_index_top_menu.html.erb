<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#"><%= image_tag(image_path('dglbxx_logo.png'))%></a>
      <div class="container nav-collapse">
        <ul class="nav pull-right">
          <li class="<%= 'active' if request.env['PATH_INFO'] == root_path %>"><a href="/">首 页</a></li>
          <% Category.roots.each_with_index do |c| %>
            <li class="<%= 'active' if params[:children_id] and Category.find(params[:children_id]).root == c %>">
              <%= link_to c.title, page_path(c, c.children.first) %>
            </li>
          <% end %>
      </div>
    </div>
  </div>
</div>